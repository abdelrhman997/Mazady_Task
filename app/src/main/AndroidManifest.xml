<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MazaadyTask"
        tools:targetApi="34">
        <activity
            android:name=".firstPage.SubmitActivity"
            android:exported="false" />
        <activity
            android:name=".secondPage.SecondPageActivity"
            android:exported="false"
            android:label="@string/title_activity_second_page" />
        <activity
            android:name=".firstPage.FirstPageActivity"
            android:exported="false" />
        <activity
            android:name=".choosePage.ChoosePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>